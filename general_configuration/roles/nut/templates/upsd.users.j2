{% for user in nut_users %}
[{{ user.name }}]
    password = {{ user.password }}
{% if user.role == 'master' %}
    actions = {{ user.actions | default('SET') }}
    instcmds = {{ user.instcmds | default('ALL') }}
    upsmon master
{% else %}
    upsmon slave
{% endif %}
{% endfor %}